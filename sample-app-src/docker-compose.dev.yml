version: "3.8"

services:
  frontend:
    build: ./frontend
    ports:
      - "8080:8080"
    environment:
      - PRODUCT_CATALOG_SERVICE_ADDR=product-catalog:3550
    depends_on:
      - product-catalog

  product-catalog:
    build: ./product-catalog
    ports:
      - "3550:3550"
